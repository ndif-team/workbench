remote = false # Whether to use NDIF remote

# List of HuggingFace model repo IDs to serve.
# Metadata (chat, gated, n_layers, params) is auto-derived at startup.
models = [
    "openai-community/gpt2",
]
